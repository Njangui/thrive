/**
 * Ré-export : source déplacée vers `src/app/_components/app-icons.tsx`
 * (sept. 2026, chantier d'unification design) — ce jeu d'icônes n'est
 * plus propre à la console Super Admin, il sert aussi la nav du
 * dashboard marchand (`/dashboard/*`). Fichier conservé ici comme simple
 * alias pour ne casser aucun `import { IconX } from "./icons"` existant
 * dans les pages `/admin/**`.
 */
export * from "@/app/_components/app-icons";
